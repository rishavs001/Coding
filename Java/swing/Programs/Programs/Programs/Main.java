public class Main {
    public static void main(String[] args) {
        Shape s = new Circle();
    }
}
/*
    We can create a subclass object assigned to a superclass variable, but the
    other way around is not possible. 

    The methods we can invoke is limited to superclass definition, but if we
    can invoke, it will invoke subclass definition
 */
