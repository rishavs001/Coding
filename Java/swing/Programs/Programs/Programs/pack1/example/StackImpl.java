public class StackImpl implements StackInterface{
    public void push(int x) {

    }

    public int pop() {
        return 0;
    }

}

/*
 * If multiple parent interfaces has same default method, but the concrete
 * implementing class overrides that method, there will be no error.
 * If the concrete implementing class does not override the method, it will give error.
 */
